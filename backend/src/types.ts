export interface UserTypes {
    id: number,
    name: string,
    email: string,
    password: string,
    isAdmin: boolean,
    createdAt: string,
    updatedAt: string,
}

export interface CategoryTypes {
    id: number,
    name: string,
    createdAt: string,
    updatedAt: string
}